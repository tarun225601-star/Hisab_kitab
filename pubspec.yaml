name: hisab_kitab
description: A smart-scale Flutter application with API key settings.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.3.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  provider: ^6.1.2

  flutter: 
    sdk: flutter
  speech_to_text: ^6.3.0    # <--- यह लाइन जोड़नी है

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
